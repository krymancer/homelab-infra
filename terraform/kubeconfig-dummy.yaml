# Placeholder so the Helm provider can configure before k3s kubeconfig exists.
# Real credentials are written to .kube/k3s-alt.yaml after first boot (gitignored).
apiVersion: v1
kind: Config
clusters:
  - cluster:
      server: https://127.0.0.1:6443
    name: dummy
contexts:
  - context:
      cluster: dummy
      user: dummy
    name: dummy
current-context: dummy
users:
  - name: dummy
    user:
      token: dummy
