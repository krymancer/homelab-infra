# Defense in depth only: keep DNS private and do not publish a tunnel/port forward.
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: lan-only
  namespace: actual
spec:
  ipAllowList:
    sourceRange:
      - 192.168.0.22/32 # Bootstrap: operator only until server password is set.
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: actual
  namespace: actual
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.middlewares: actual-lan-only@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - actual.homelab.krymancer.dev
      secretName: homelab-wildcard-tls
  rules:
    - host: actual.homelab.krymancer.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: actual
                port:
                  name: http
